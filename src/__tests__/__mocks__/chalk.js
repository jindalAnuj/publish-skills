module.exports = {
  default: {
    red: (text) => text,
    green: (text) => text,
    yellow: (text) => text,
    cyan: (text) => text,
    gray: (text) => text,
    bold: (text) => text,
    underline: (text) => text,
  },
  red: (text) => text,
  green: (text) => text,
  yellow: (text) => text,
  cyan: (text) => text,
  gray: (text) => text,
  bold: (text) => text,
  underline: (text) => text,
};
